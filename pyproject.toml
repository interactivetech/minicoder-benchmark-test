[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "minicoder-benchmark-test"
version = "0.1.0"
description = "Reproducible mini-coder-1.7b and mini-SWE-agent benchmark deployment"
requires-python = ">=3.10"
dependencies = [
  "mini-swe-agent==2.4.6",
]

[project.optional-dependencies]
evaluation = [
  "swebench==5.0.2",
]

[tool.setuptools]
py-modules = []
